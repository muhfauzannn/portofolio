import { PlaceholderPage } from "@/features/placeholder";

export default function ContactRoute() {
  return (
    <PlaceholderPage
      eyebrow="Say hello"
      title="Contact"
      description="A mockup route to test page transitions — a real form lands here soon."
    />
  );
}
